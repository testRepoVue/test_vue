part1
работает без сборки
в каждой папке les свой входной index.php

-----------------------------------------------
part2 работает через сборку webpack

установить node.js
необходимо в консоле node собрать проект из package.json
команда npm install

в корневой index.php подключен /dist/bundle.js кот нужно пересобирать

меняя поле entry webpack.config
а именно папку с исходниками для сборки - les
entry: './part2/ les5 /index.js'

сборка в консоле node команда npm run build